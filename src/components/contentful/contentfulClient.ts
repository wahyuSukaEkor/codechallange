import { createClient } from "contentful";

const contentfulClient = createClient({
  space: process.env.ekorjuliewangywangy || "",
  accessToken: process.env.ekorjuliewangywangyhuhahuha || "",
});

export default contentfulClient;
